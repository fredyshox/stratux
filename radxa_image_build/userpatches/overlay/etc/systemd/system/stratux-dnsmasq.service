[Unit]
Description=Stratux DHCP/DNS for WiFi AP
After=stratux-wifi.service
Requires=stratux-wifi.service

[Service]
Type=simple
ExecStart=/usr/sbin/dnsmasq -k -u dnsmasq --conf-dir=/etc/dnsmasq.d -i wlan0
Restart=on-failure
RestartSec=5

[Install]
WantedBy=multi-user.target
