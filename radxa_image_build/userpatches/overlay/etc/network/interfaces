auto lo

iface lo inet loopback

allow-hotplug eth0
iface eth0 inet dhcp

# USB Ethernet dongles (predictable naming)
allow-hotplug enx*
iface enx* inet dhcp

# wlan0 is managed by stratux-wifi.service, not ifupdown
